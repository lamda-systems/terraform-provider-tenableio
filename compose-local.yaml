services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-local
    init: false
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
    environment:
      - GOPATH=/go
      - GOCACHE=/go/cache
